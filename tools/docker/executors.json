{
  "executors": {
    "build": {
      "implementation": "./src/executors/build/build.ts",
      "schema": "./src/executors/build/schema.json",
      "description": "Build docker image using Docker CLI"
    },
    "push": {
      "implementation": "./src/executors/push/push.ts",
      "schema": "./src/executors/push/schema.json",
      "description": "Push docker image to registry using Docker CLI"
    }
  }
}
