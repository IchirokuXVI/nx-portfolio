{
  "executors": {
    "build": {
      "implementation": "./src/build/build.ts",
      "schema": "./src/build/schema.json",
      "description": "Build docker image using Docker CLI"
    },
    "push": {
      "implementation": "./src/push/push.ts",
      "schema": "./src/push/schema.json",
      "description": "Push docker image to registry using Docker CLI"
    }
  }
}
