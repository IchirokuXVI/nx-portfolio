# odontogram/data-access

The odontogram data access services (odontogram, tooth treatment and treatment), their in memory implementations, an API implementation of the odontogram service, and the providers that bind the in memory ones.

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test odontogram/data-access` to execute the unit tests.
