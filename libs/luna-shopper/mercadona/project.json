{
  "name": "luna-shopper/mercadona",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/luna-shopper/mercadona/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": [],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/luna-shopper/mercadona/jest.config.cts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "capture-fixtures": {
      "//": "Refreshes the checked in fixtures from the live API (plan 0038, section 9). Never run by CI: it makes real requests, and the fixtures are what keeps every other test off the network.",
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsx libs/luna-shopper/mercadona/tools/capture-fixtures.ts"
      }
    }
  }
}
