# shared/ui

Presentational components shared across apps, including the icon components. Check this library before adding a new icon.

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test shared/ui` to execute the unit tests.
