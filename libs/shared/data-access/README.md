# shared/data-access

The shared API URL resolver, the API consumer helpers and the service token functions built on `@portfolio/shared/environments`.

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test shared/data-access` to execute the unit tests.
