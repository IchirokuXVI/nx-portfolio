# Damocle'Sword — Case Study

> Answers (`A:`) are written by Daniel. `> Note (Claude):` blocks flag things the
> code shows that an answer may have missed.

## The project

**Q: What is Damocle'Sword? What real work does this remote showcase (Starlit Ascension, the VR demos)?**
A:

**Q: What were you trying to make a visitor feel / understand on this page?**
A:

## Data & content

**Q: The data-access layer uses a "memory" pattern (`*-memory.ts` + `*-service.ts` + `static-*-data.ts`). Why this shape rather than plain constants or a real API?**
A:

**Q: Content is split into static data + separate per-locale translation data (`static-news-translation-data.ts`, etc.). How does that work and why split it?**
A:

## Media

**Q: The app ships mp4 trailers and avif images. How do you handle these large assets (format choice, loading, the "equal media sizes" rule you enforced)?**
A:

## Features

**Q: feature-home — notable implementation details (hex background, layout, the showcase hero)?**
A:

**Q: feature-news — how the news list/section is built and localized?**
A:

**Q: feature-contact — is the contact form wired to a real backend or mocked (`contact-mock.ts`)? What happens on submit?**
A:

**Q: feature-about / projects — anything worth explaining?**
A:
