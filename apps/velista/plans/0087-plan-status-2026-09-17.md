# 0087: where the plans stand on 2026-09-17

> **This plan builds nothing.** It is a status report, written on 2026-09-17 against `dev` at
> `b2977278`, for a later session that re-evaluates the plans it names. It records what was
> found. It does not decide what happens next.

## Brief for the agent

- **Objective:** re-evaluate velista 0064 and 0070 against the code as it stands on the day you
  read this, and write a new plan for each need that survives.
- **Context:** both plans were written and then overtaken by later work. **Neither will be built
  as written.** Each carries a status header that points here. 0070 is the client half of the
  backend measurement series (backend 0095, 0096, 0101, 0102), which is pending re-evaluation for
  the same reason, in backend plan 0121. Re-evaluate the two together.
- **Target state:** for each plan, either a new plan in `plans/` (next free number) that restates
  the surviving need against the current code, or a written decision that the need is gone. The
  old file then says which one, in its status header.
- **Scope:** `apps/velista/plans/`. Read code anywhere.
- **Do not touch:** application code. Re-evaluation writes plans, not features.
- **Constraints:** the facts below were true on 2026-09-17. Check each one again before relying on
  it.
- **Stop and ask before:** deciding 0070's fate without the backend series decided first, because
  0070 draws what the backend series sends.
- **Progress evidence:** the decision for each plan, and the new plan numbers.

## 0. How this was found

Every velista plan was checked against the code and the git history. Each built plan got a header
in the same pull request as this report: a PR link where a feature pull request merged it into
`dev`, and `> **Status: implemented.**` with the commit where the work went straight to `dev`
(0001 to 0034 mostly, from before the pull request workflow).

**Built:** 0001 to 0049, 0051 to 0056, 0058 to 0063, 0065 to 0069 and 0071 to 0086. There is no
0057.

**Superseded:** 0050, specs for the basket and an e2e that shops. 0064 took over the specs it
still owed, and 0080 (#370) built the e2e against a real backend. Its header says so.

## 1. 0064 What 0050 still owes: partly built, not to be built as written

- **Written:** 2026-09-03, commit `502f52b45`, PR #160 (plan only).
- **What it proposed:** an audit based second attempt at 0050's specs, plus one owner and guest
  e2e, with the CI placement left open.
- **What exists now, deliverable by deliverable:**

| Deliverable | State on 2026-09-17 |
| ----------- | ------------------- |
| `join-page.spec.ts` | missing. `libs/velista/feature-shopping-lists/src/lib/join-page/` holds only `.html`, `.scss` and `.ts` |
| `basket-page.spec.ts`, guest cast and a privileged cast that loses its view | file exists with guest cases (face row, finishing, banner). No case for a privileged cast losing `WRITE` on the next request |
| `share-sheet.spec.ts`, revoke semantics | file exists (14 cases), about the revoke pane and double tap from 0052. The four revoke rules 0064 lists were not found as cases |
| `BasketApi`, three credential outcomes | `basket-api.spec.ts` exists (`join`, `renameLine`). 0 hits for `PARTICIPANT_SECRET_HEADER` or `x-participant-secret` in it |
| `basket-session-store.spec.ts` | missing. `basket-session-store.ts` exists |
| backend `GeneratedListBasketService` spec (`getBasket`, `setPick`) | missing. Core has `generated-list-basket-add.spec.ts` and `generated-list-basket-settled.spec.ts`, the gateway has `generated-list-basket-prices.spec.ts` |
| `sourceNames` composition spec | missing. Only `generated-list-shared-people.integration.spec.ts` mentions `sourceNames` |
| owner and guest e2e with revoke, no reload | built by 0080 as `apps/velista-luna-e2e/src/guest.spec.ts` (partial settle, split, guest sees no list names, people sheet, revoke with cascade refused), beside `member.spec.ts` and `shop.spec.ts` |
| a CI gate that holds both halves | built by 0080: a separate browser gate in `.github/workflows/docker-ci.yml` (about lines 200 to 239). 0064's open question (extend or add a gate) is answered |

- **What changed since:** 0080 (#370) and 0086 (#372) built and hardened the shopping e2e. 0084
  (#380) last touched `basket-api.spec.ts`. 0085 (#382) and backend 0114 (#369) changed how a
  basket is shared, which the revoke and credential specs describe.

## 2. 0070 A weighed product has a till line: not built, not to be built as written

- **Written:** 2026-09-06 in PR #250, rewritten 2026-09-09 in PR #305.
- **What it proposed:** `MEASUREMENT_UNITS` and `PRICE_BASES` in `enums.ts`. `comparablePrice`,
  `comparableUnit` and `comparableSource` on `ProductOffer`. `measurementUnit`, `quantitySteps`
  and `equivalences` on `CatalogItem`, `BasketProduct` and `ProductGroup`, and a unit on lines.
  `formatQuantity`, `moneyCents` and `lineAmount` (returning `cents`, `mark`, `packs`) beside
  `money.ts`. An "≈" estimate mark (`basket.amount.estimate`), unit suffixes on the row, amounts on
  the settle sheet, `unit` and `steps` inputs on `QuantityReel`, a unit chip on the units sheet and
  the line edit sheet, and a unit on `LineService.addQuantity` with a silent 409 and a raised
  client floor.
- **Depends on:** backend 0095, 0096, 0101 and 0102, all unbuilt and pending re-evaluation. Also
  velista 0062, 0063 and 0054, which are built.
- **Not built:** 0 hits for `lineAmount`, `formatQuantity`, `MEASUREMENT_UNITS` and `PRICE_BASES`.
- **What changed since:**
  - 0073 (#328), with backend 0104, changed the line sheet and the reel.
  - 0074 to 0079 (#341, #345, #346, #348, #353, #362) added the tools row, the filter sheet,
    grouping, and prices from one shop (backend 0109), in the same row and settle sheet area
    0070 draws into.
  - 0083 (#377) rebuilt the line edit sheet 0070 targets.
  - 0084 (#380) and 0085 (#382) changed renames and sharing.
  - 0072 (#269) built the client floor mechanism 0070 raises.
- **Open items the plan listed:** no basket total, no fractional quantities.
