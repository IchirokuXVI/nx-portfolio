# 0031 Where the plans stand on 2026-09-17

> **This plan builds nothing.** It is a status report, written on 2026-09-17 against `dev` at
> `b2977278`, for a later session. It records what was found. It does not decide what happens
> next.

## Brief for the agent

- **Objective:** know which back office plans are still unbuilt, and what each one waits on.
- **Context:** every plan in this directory up to 0028 is built. Two are not, and both are recent
  and still in the build order. Unlike velista 0070 and the backend measurement series, **nothing
  here is marked for rewriting**.
- **Constraints:** the facts below were true on 2026-09-17. Check each one again before relying on
  it.
- **Progress evidence:** none needed. This file is context for picking up 0029 or 0030.

## 0. How this was found

Every plan in this directory was checked against the code and the git history. 0001 (#170) and
0002 (#176) were the only built plans without a PR header, and both got one in the same pull
request as this report. 0003 to 0028 already carried theirs. 0028 was built in #387, merged on
2026-09-17.

## 1. 0029 A run that copies its walks: not built, prerequisites met

- **Written:** 2026-09-16, PR #376 (plan only).
- **What it proposes:** on `RunsPage` (`libs/luna-shopper-admin/feature-harvest/src/lib/runs-page.ts`)
  read `LOCAL_AREA`, `REGION` and `NATIONAL` scopes, a "Copy to" chip list per walked scope using
  0028's `references` control, `scopeCopies`, a `writes` choice and a `details` choice shown when
  `skipsKnownDetails` applies. A "Copied from" column on the catalog price views. A report with the
  copies and the warnings `COPY_TARGET_GONE`, `COPY_SOURCE_NOT_WRITTEN` and
  `DETAIL_SKIPPED_UNKNOWN`.
- **Not built:** outside the generated wire types, 0 hits in `apps/luna-shopper-admin` and
  `libs/luna-shopper-admin` for `scopeCopies`, `skipsKnownDetails`, `copiedFromScopeId` and
  `priceCopiedFromScopeId`.
- **Prerequisites, all merged:** backend 0116 (#383, a repeatable `kind` on the scope list query),
  0118 (#384) and 0119 (#386), whose fields are already in `wire-types.ts`, and admin 0028 (#387,
  `@case ('references')` in `field-control.ts`).

## 2. 0030 Run presets: not built, blocked

- **Written:** 2026-09-16, PR #376 (plan only).
- **What it proposes:** a `RunRequestForm` extracted from `RunsPage`, a `PresetsPage` at `presets`
  in `HARVEST_LINKS`, a "Save as preset" dialog, runs shown and filtered by `presetId`, and preset
  methods on `HarvestService` with the memory fake and seed.
- **Not built:** 0 hits for `RunRequestForm`, `PresetsPage` and `presetId`.
- **Blocked on:** backend 0120 (unbuilt, see backend plan 0121 section 2) and admin 0029.
