{
  "name": "docker/reverse-proxy",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/docker/reverse-proxy/src",
  "targets": {
    "build": {
      "executor": "@portfolio/docker:build",
      "options": {
        "imageName": "nx-portfolio/reverse-proxy"
      },
      "configurations": {
        "development": {
          "tag": "dev"
        },
        "production": {
          "pushToRegistry": true
        }
      }
    }
  }
}
