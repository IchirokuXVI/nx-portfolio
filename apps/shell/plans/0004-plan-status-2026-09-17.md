# 0004 Where the plans stand on 2026-09-17

> **This plan builds nothing.** It is a status report, written on 2026-09-17 against `dev` at
> `b2977278`, for a later session. It records what was found. It does not decide what happens
> next.

## Brief for the agent

- **Objective:** know what is still open in the shell plans.
- **Context:** 0002 and 0003 are built and 0001, the case study documents plan, is partly done.
  0001 is documentation, not code, so what is missing is answers in `CASE_STUDY.md` files.
- **Constraints:** the facts below were true on 2026-09-17. Check each one again before relying on
  it.

## 0. How this was found

Every plan here was checked against the code and the git history. 0002 (commit `2a9ef171a`) and
0003 (commit `9c8561147`) went straight to `dev`, and both now carry `> **Status: implemented.**`
with their commit. 0001 carries `> **Status: partly done.**`. Its work arrived through the
`worktree-case-study-docs` branch, merged into `dev` by `a43a4e278` on 2026-08-25 with no pull
request.

## 1. 0001 Case study docs: the questions still open

All shell and docker questions are answered, and `apps/shell/CASE_STUDY.md`,
`apps/docker/CASE_STUDY.md`, `apps/damoclesSword/CASE_STUDY.md` and `apps/odontogram/CASE_STUDY.md`
exist.

| Mark | Question (line in the plan) | File |
| ---- | --------------------------- | ---- |
| `[ ]` | What is Damocle'Sword? The real project this showcases (Starlit Ascension, VR). (139) | `apps/damoclesSword/CASE_STUDY.md` |
| `[ ]` | The data access "memory" pattern (`*-memory.ts` + `*-service.ts` + static data). Why? (140) | same |
| `[ ]` | How static content and per locale translation data are structured and served. (141) | same |
| `[ ]` | Media (mp4, avif): large assets, why avif, the equal media size rule. (142) | same |
| `[ ]` | feature-home, about, news, projects, contact: notable details of each. (143) | same |
| `[ ]` | Contact form: wired to a backend? contact-mock against the real service. (144) | same |
| `[~]` | Image preloading: images loaded through JS, the chart shown only once all load. (164) The rendering answer covers it, but says little about waiting for all of them | `apps/odontogram/CASE_STUDY.md` |
| `[ ]` | Original purpose against landingV2, and what is kept or merged. (186) | `apps/landing/CASE_STUDY.md`, which does not exist |
| `[ ]` | The projects, areas and project-areas data model. (187) | same |

The last two questions are about the `landing` remote, which plan 0002 retired: `apps/landing` no
longer exists. They have no file to live in.

The damoclesSword file is 39 lines and answers none of its six questions.
